# 2026-08-14

## 部署 WorkBuddy 插件到本机

执行 `npx --yes huaweicloud-devkit@next install --target workbuddy`（@next = 1.0.2-dev.8，已包含 workbuddy 支持）完成本机安装：

- Skills → `~/.workbuddy/skills/`（28 个，含 huawei-sandbox）
- MCP Server → `~/.workbuddy/huaweicloud-plugins/src/`
- Safety Policy → `~/.workbuddy/huaweicloud-plugins/safety/`
- MCP 配置 → `~/.workbuddy/mcp.json`（自动注入 HCLOUD_BIN）

doctor 自检 8 项全通过（hcloud 7.2.12 已就绪、凭证已配置）。

### 遗留小瑕疵（待修）

doctor 输出的路径检测优先命中了 OpenCode（本机也装过 OpenCode 版本），导致提示文案显示「重启 OpenCode」而非「重启 WorkBuddy」。功能不受影响，但文案有误导。根因：`cmdDoctor()` 的 `mcpTarget` 判断顺序为 OpenCode → Codex Desktop → WorkBuddy，应支持 `--target workbuddy` 精确指定或按最近安装目标优先。
